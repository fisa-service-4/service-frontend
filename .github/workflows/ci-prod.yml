name: CI-Prod

on:
  push:
    branches:
      - prod

permissions:
  contents: read
  packages: write

env:
  SERVICE_NAME: service-frontend

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Login GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push (prod tag)
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ghcr.io/${{ github.repository }}:prod
          build-args: |
            TRANSACTION_URL=${{ vars.TRANSACTION_URL }}

  deploy-app-2a:
    runs-on: ubuntu-latest
    needs: build-and-push
    steps:
      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.AWS_SSH_PRIVATE_KEY }}" > ~/.ssh/aws_key.pem
          chmod 600 ~/.ssh/aws_key.pem

      - name: Deploy to APP-2A
        run: |
          ssh -i ~/.ssh/aws_key.pem \
            -o StrictHostKeyChecking=no \
            -o ProxyCommand="ssh -i ~/.ssh/aws_key.pem -W %h:%p -o StrictHostKeyChecking=no ${{ secrets.AWS_BASTION_USER }}@${{ secrets.AWS_BASTION_HOST }}" \
            ubuntu@172.16.10.10 << 'EOF'
          cd ~/infra && git pull
          IMAGE_TAG=prod docker compose -f docker-compose-prod-app.yml pull ${{ env.SERVICE_NAME }}
          IMAGE_TAG=prod docker compose -f docker-compose-prod-app.yml up -d --no-deps ${{ env.SERVICE_NAME }}
          docker image prune -f
          EOF

      # TODO: 실제 헬스체크 엔드포인트 확인 후 주석 해제
      # - name: Health Check — APP-2A
      #   run: |
      #     ssh -i ~/.ssh/aws_key.pem \
      #       -o StrictHostKeyChecking=no \
      #       -o ProxyCommand="ssh -i ~/.ssh/aws_key.pem -W %h:%p -o StrictHostKeyChecking=no ${{ secrets.AWS_BASTION_USER }}@${{ secrets.AWS_BASTION_HOST }}" \
      #       ubuntu@172.16.10.10 \
      #       '
      #       for i in $(seq 1 12); do
      #         if curl -sf -o /dev/null http://localhost:3000; then
      #           echo "Service UP"; exit 0
      #         fi
      #         echo "Waiting... ($i/12)"; sleep 10
      #       done
      #       echo "Health check failed"; exit 1
      #       '

  deploy-app-2c:
    runs-on: ubuntu-latest
    needs: deploy-app-2a
    steps:
      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.AWS_SSH_PRIVATE_KEY }}" > ~/.ssh/aws_key.pem
          chmod 600 ~/.ssh/aws_key.pem

      - name: Deploy to APP-2C
        run: |
          ssh -i ~/.ssh/aws_key.pem \
            -o StrictHostKeyChecking=no \
            -o ProxyCommand="ssh -i ~/.ssh/aws_key.pem -W %h:%p -o StrictHostKeyChecking=no ${{ secrets.AWS_BASTION_USER }}@${{ secrets.AWS_BASTION_HOST }}" \
            ubuntu@172.16.11.10 << 'EOF'
          cd ~/infra && git pull
          IMAGE_TAG=prod docker compose -f docker-compose-prod-app.yml pull ${{ env.SERVICE_NAME }}
          IMAGE_TAG=prod docker compose -f docker-compose-prod-app.yml up -d --no-deps ${{ env.SERVICE_NAME }}
          docker image prune -f
          EOF

      # TODO: 실제 헬스체크 엔드포인트 확인 후 주석 해제
      # - name: Health Check — APP-2C
      #   run: |
      #     ssh -i ~/.ssh/aws_key.pem \
      #       -o StrictHostKeyChecking=no \
      #       -o ProxyCommand="ssh -i ~/.ssh/aws_key.pem -W %h:%p -o StrictHostKeyChecking=no ${{ secrets.AWS_BASTION_USER }}@${{ secrets.AWS_BASTION_HOST }}" \
      #       ubuntu@172.16.11.10 \
      #       '
      #       for i in $(seq 1 12); do
      #         if curl -sf -o /dev/null http://localhost:3000; then
      #           echo "Service UP"; exit 0
      #         fi
      #         echo "Waiting... ($i/12)"; sleep 10
      #       done
      #       echo "Health check failed"; exit 1
      #       '
